import AuthPanel from '../../components/AuthPanel';

export const metadata = { title: 'Login — HidakaXin' };

export default function LoginPage() {
  return (
    <div className="min-h-[70vh]">
      <AuthPanel />
    </div>
  );
}